---
phase: 02-upravlenie-pajplajnom
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - bratsy-tauri/src/main.js
  - bratsy-tauri/src/index.html
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - UI-01
  - UI-03

must_haves:
  truths:
    - "Клик по карточке запускает этап (invoke run_stage) если он не запущен"
    - "Повторный клик по активной карточке останавливает процесс (invoke stop_stage)"
    - "JS обновляет пилл и иконку при получении stage-status события"
    - "Строки лога появляются в лог-панели при получении stage-log события"
    - "Лог-панель скрыта когда нет активного этапа, появляется при запуске"
    - "Toast появляется при статусе done или error"
  artifacts:
    - path: "bratsy-tauri/src/main.js"
      provides: "event listeners, toggle логика, updatePill(), appendLog(), showToast()"
      contains: "window.__TAURI__.event.listen"
    - path: "bratsy-tauri/src/index.html"
      provides: "лог-панель #logPanel, toast контейнер #toastContainer"
      contains: "id=\"logPanel\""
  key_links:
    - from: "stage-status Tauri event"
      to: "updatePill(stage, status)"
      via: "listen() callback"
      pattern: "listen.*stage-status"
    - from: "stage-log Tauri event"
      to: "appendLog(stage, line)"
      via: "listen() callback"
      pattern: "listen.*stage-log"
    - from: "stage-card click"
      to: "invoke('run_stage') or invoke('stop_stage')"
      via: "activeStages Set toggle"
      pattern: "activeStages"
---

<objective>
JS-слой: подписка на Tauri события, toggle-паттерн запуска/остановки, обновление UI в реальном времени, лог-панель, toast-уведомления.

Purpose: Соединить Rust-бэкенд (Plan 01) с видимым UI — пользователь видит статус в реальном времени без перезагрузки.
Output: Обновлённые main.js и index.html с полным интерактивным pipeline UI.
</objective>

<execution_context>
@e:/Bratsy_DP/.claude/get-shit-done/workflows/execute-plan.md
@e:/Bratsy_DP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@e:/Bratsy_DP/.planning/ROADMAP.md
@e:/Bratsy_DP/.planning/REQUIREMENTS.md
@e:/Bratsy_DP/.planning/phases/02-upravlenie-pajplajnom/02-CONTEXT.md
@e:/Bratsy_DP/.planning/phases/02-upravlenie-pajplajnom/02-01-SUMMARY.md

<interfaces>
<!-- Контракты из Plan 01 (Rust события) -->

События от Rust → JS:
```
"stage-status"  payload: { stage: string, status: "running"|"done"|"error" }
"stage-log"     payload: { stage: string, line: string }
```

Rust команды (invoke):
```
invoke('run_stage', { stage: string })   → Promise<void>
invoke('stop_stage', { stage: string })  → Promise<void>
```

Существующие CSS классы (из styles.css):
```
.pill-ready   — зелёный фон (готов к запуску)
.pill-active  — синий фон (активен/ожидает, используется в prototype)
.pill-idle    — серый фон

.stage-card         — базовая карточка
.stage-card.stage-active — синяя рамка (уже существует)

.dot-green, .dot-blue, .dot-gray — статусные точки
```

Новые классы которые будет добавлять CSS Plan 03:
```
.pill-running  — синий (запущен)
.pill-done     — зелёный (завершён)
.pill-error    — красный (ошибка)
.dot-red       — красная точка
.stage-card.stage-running — активная с hover-red
```

Существующий HTML (data-stage атрибуты):
```html
<div class="stage-card" data-stage="autocad"> ... <div class="stage-pill pill-ready">...</div> </div>
<div class="stage-card" data-stage="pdm">     ... </div>
<div class="stage-card" data-stage="excel">   ... </div>
<div class="stage-card stage-active" data-stage="plantsim"> ... </div>
<div class="stage-card" data-stage="report">  ... </div>
```

Icon SVG в карточке (будем менять при running):
```html
<!-- иконка-заглушка для стоп ■ -->
<svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
  <rect x="4" y="4" width="16" height="16" rx="2"/>
</svg>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTML — лог-панель и toast контейнер</name>
  <files>
    bratsy-tauri/src/index.html
  </files>
  <read_first>
    - bratsy-tauri/src/index.html (прочитан в планировании — структура известна)
  </read_first>
  <action>
Добавить два новых блока в index.html.

**1. Лог-панель** — вставить ПОСЛЕ блока `<!-- Pipeline stages -->` (после закрывающего `</div>` секции `.stages`) и ПЕРЕД `<!-- Footer -->`:

```html
<!-- Log panel -->
<div class="log-panel" id="logPanel">
  <div class="log-header">
    <span class="log-dot"></span>
    <span class="log-title" id="logTitle">Лог</span>
  </div>
  <div class="log-body" id="logBody"></div>
</div>
```

**2. Toast контейнер** — вставить в конец `<body>` ПЕРЕД `<script src="main.js" ...>`:

```html
<!-- Toast notifications -->
<div class="toast-container" id="toastContainer"></div>
```

**3. Убрать** класс `stage-active` с карточки plantsim в HTML (он теперь управляется JS динамически):
```html
<!-- было: <div class="stage-card stage-active" data-stage="plantsim"> -->
<!-- стало: -->
<div class="stage-card" data-stage="plantsim">
```

Также убрать статический класс `pill-active` с пилла plantsim, заменить на `pill-ready`:
```html
<!-- было: <div class="stage-pill pill-active"><span class="dot dot-blue"></span>Active</div> -->
<!-- стало: -->
<div class="stage-pill pill-ready"><span class="dot dot-green"></span>Ready</div>
```
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'e:\Bratsy_DP\bratsy-tauri\src\index.html' -Pattern 'id=\"logPanel\"','id=\"toastContainer\"','id=\"logBody\"','id=\"logTitle\"' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <done>
    index.html содержит `id="logPanel"`, `id="logBody"`, `id="logTitle"`, `id="toastContainer"`.
    Карточка plantsim НЕ имеет класса `stage-active` в HTML (управляется JS).
  </done>
</task>

<task type="auto">
  <name>Task 2: main.js — события, toggle, лог, toast</name>
  <files>
    bratsy-tauri/src/main.js
  </files>
  <read_first>
    - bratsy-tauri/src/main.js (прочитан в планировании)
  </read_first>
  <action>
Полная замена секции `// ── Stage cards ───────────────────────────────────────────────`
и добавление новых секций в конец файла (перед `// ── Load settings on start`).

**Шаг 1:** В начало файла добавить import для Tauri event API:
```javascript
const { invoke } = window.__TAURI__.core;
const { listen } = window.__TAURI__.event;
```
(Заменяет существующую строку `const { invoke } = window.__TAURI__.core;`)

**Шаг 2:** Заменить секцию `// ── Stage cards` целиком:

```javascript
// ── Stage control (toggle: click = run, click again = stop) ──────
const activeStages = new Set();   // stage IDs которые сейчас running

// Метка этапа для заголовка лога (data-stage → читабельное имя)
const STAGE_LABELS = {
  autocad:  'AutoCAD',
  pdm:      'Vault PDM',
  excel:    'Excel',
  plantsim: 'Plant Simulation',
  report:   'Report',
};

document.querySelectorAll('.stage-card').forEach(card => {
  card.addEventListener('click', async () => {
    const stage = card.dataset.stage;

    if (activeStages.has(stage)) {
      // Повторный клик — остановить
      try {
        await invoke('stop_stage', { stage });
      } catch (e) {
        console.error('stop_stage error:', e);
      }
    } else {
      // Первый клик — запустить
      totalAttempts++;
      clearLog();
      setLogTitle(STAGE_LABELS[stage] || stage);
      showLogPanel(true);
      try {
        await invoke('run_stage', { stage });
        lastSyncTime = Date.now();
      } catch (e) {
        failedAttempts++;
        console.error('run_stage error:', e);
        updatePill(stage, 'error');
        showToast(STAGE_LABELS[stage] || stage, 'error');
      }
    }
  });
});
```

**Шаг 3:** Добавить функции управления UI после секции stage cards:

```javascript
// ── Pill & card state update ──────────────────────────────────────
const PILL_MAP = {
  waiting: { cls: 'pill-ready',   dot: 'dot-green', text: 'Ready' },
  running: { cls: 'pill-running', dot: 'dot-blue',  text: 'Запущен' },
  done:    { cls: 'pill-done',    dot: 'dot-green', text: 'Завершён' },
  error:   { cls: 'pill-error',   dot: 'dot-red',   text: 'Ошибка' },
};

function updatePill(stage, status) {
  const card = document.querySelector(`.stage-card[data-stage="${stage}"]`);
  if (!card) return;

  const pill = card.querySelector('.stage-pill');
  if (!pill) return;

  const cfg = PILL_MAP[status] || PILL_MAP.waiting;

  // Снять все pill-* классы
  pill.className = `stage-pill ${cfg.cls}`;
  pill.innerHTML = `<span class="dot ${cfg.dot}"></span>${cfg.text}`;

  // Обновить состояние карточки
  if (status === 'running') {
    card.classList.add('stage-running');
    card.classList.remove('stage-active');
    activeStages.add(stage);
    // Иконка → ■ стоп
    setCardStopIcon(card, true);
  } else {
    card.classList.remove('stage-running');
    card.classList.remove('stage-active');
    activeStages.delete(stage);
    setCardStopIcon(card, false);
  }

  // Скрыть лог-панель если все этапы остановлены
  if (activeStages.size === 0 && (status === 'done' || status === 'error')) {
    // Небольшая задержка чтобы пользователь увидел финальный статус
    setTimeout(() => {
      if (activeStages.size === 0) showLogPanel(false);
    }, 3000);
  }
}

// Иконка стоп (■) внутри stage-icon-wrap
function setCardStopIcon(card, isStop) {
  const wrap = card.querySelector('.stage-icon-wrap');
  if (!wrap) return;

  if (isStop) {
    wrap.dataset.origHtml = wrap.innerHTML;
    wrap.innerHTML = `<svg width="28" height="28" viewBox="0 0 24 24" fill="#C0392B"><rect x="4" y="4" width="16" height="16" rx="3"/></svg>`;
  } else {
    if (wrap.dataset.origHtml) {
      wrap.innerHTML = wrap.dataset.origHtml;
      delete wrap.dataset.origHtml;
    }
  }
}
```

**Шаг 4:** Добавить функции лог-панели:

```javascript
// ── Log panel ─────────────────────────────────────────────────────
const LOG_MAX_LINES = 200;
let logLines = [];

function showLogPanel(visible) {
  const panel = document.getElementById('logPanel');
  if (visible) {
    panel.classList.add('visible');
  } else {
    panel.classList.remove('visible');
  }
}

function setLogTitle(stageName) {
  const el = document.getElementById('logTitle');
  if (el) el.textContent = `● ${stageName} — лог`;
}

function clearLog() {
  logLines = [];
  const body = document.getElementById('logBody');
  if (body) body.innerHTML = '';
}

function appendLog(stage, line) {
  const body = document.getElementById('logBody');
  if (!body) return;

  const now = new Date();
  const ts = [
    String(now.getHours()).padStart(2, '0'),
    String(now.getMinutes()).padStart(2, '0'),
    String(now.getSeconds()).padStart(2, '0'),
  ].join(':');

  logLines.push(line);
  if (logLines.length > LOG_MAX_LINES) {
    logLines.shift();
    body.firstChild?.remove();
  }

  const row = document.createElement('div');
  row.className = 'log-line';
  row.innerHTML = `<span class="log-ts">${ts}</span><span class="log-text">${escapeHtml(line)}</span>`;
  body.appendChild(row);

  // Auto-scroll вниз
  body.scrollTop = body.scrollHeight;
}

function escapeHtml(str) {
  return str
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;');
}
```

**Шаг 5:** Добавить функции toast:

```javascript
// ── Toast notifications ───────────────────────────────────────────
function showToast(stageName, type) {
  const container = document.getElementById('toastContainer');
  if (!container) return;

  const toast = document.createElement('div');
  const isDone = type === 'done';
  toast.className = `toast toast-${isDone ? 'done' : 'error'}`;
  toast.textContent = isDone
    ? `«${stageName}» завершён`
    : `«${stageName}» — ошибка`;

  container.appendChild(toast);

  // Trigger reflow для CSS transition
  toast.getBoundingClientRect();
  toast.classList.add('toast-visible');

  // Авто-исчезновение через 4 сек
  setTimeout(() => {
    toast.classList.remove('toast-visible');
    toast.classList.add('toast-hiding');
    setTimeout(() => toast.remove(), 400);
  }, 4000);
}
```

**Шаг 6:** Добавить Tauri event listeners в `DOMContentLoaded`. Найти существующий блок:
```javascript
window.addEventListener('DOMContentLoaded', async () => {
  try {
    const s = await invoke('get_settings');
    ...
  }
});
```
И добавить listeners в начало этого же обработчика:

```javascript
window.addEventListener('DOMContentLoaded', async () => {
  // Tauri event listeners
  await listen('stage-status', (event) => {
    const { stage, status } = event.payload;
    updatePill(stage, status);

    if (status === 'done' || status === 'error') {
      const label = STAGE_LABELS[stage] || stage;
      setLogTitle(`${label} — ${status === 'done' ? 'Завершён' : 'Ошибка'}`);
      showToast(label, status);
      if (status === 'done') lastSyncTime = Date.now();
      else failedAttempts++;
    }
  });

  await listen('stage-log', (event) => {
    const { stage, line } = event.payload;
    appendLog(stage, line);
  });

  // Загрузить настройки (существующий код)
  try {
    const s = await invoke('get_settings');
    if (s.plant_sim_path) document.getElementById('inputPlantSim').value = s.plant_sim_path;
    if (s.work_dir)       document.getElementById('inputWorkDir').value  = s.work_dir;
    if (s.scripts_dir)    document.getElementById('inputScripts').value  = s.scripts_dir;
  } catch (e) {
    console.warn('Could not load settings:', e);
  }
});
```

**Шаг 7:** Убрать обработчик `runPipeline` (кнопка остаётся в HTML, функциональность в Phase 3):
```javascript
// ── Run full pipeline — заглушка до Phase 3 ───────────────────────
document.getElementById('runPipeline')?.addEventListener('click', () => {
  console.info('Full pipeline: будет реализовано в Phase 3');
});
```
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'e:\Bratsy_DP\bratsy-tauri\src\main.js' -Pattern 'listen.*stage-status','activeStages','updatePill','appendLog','showToast','showLogPanel' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <done>
    main.js содержит `listen('stage-status'`, `listen('stage-log'`, `activeStages`, `updatePill`, `appendLog`, `showToast`, `showLogPanel`.
    Существующий код uptime/metrics и settings panel сохранён без изменений.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri event → JS DOM | данные из Rust события рендерятся в HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | XSS | appendLog: line из Rust рендерится в DOM | mitigate | Использовать `escapeHtml()` для экранирования `<`, `>`, `&` перед вставкой в innerHTML. Реализовано в функции appendLog через .innerHTML = `..${escapeHtml(line)}..` |
| T-02-05 | DoS | appendLog: процесс генерирует тысячи строк | accept | LOG_MAX_LINES = 200 ограничивает DOM-рост. Старые строки удаляются через `body.firstChild?.remove()` |
</threat_model>

<verification>
После выполнения обоих tasks:

1. Открыть приложение (`cargo tauri dev` или собранный exe)
2. Кликнуть на карточку — пилл должен смениться на "Запущен" (синий)
3. Лог-панель должна появиться с строками лога
4. Через ~2 сек пилл должен смениться на "Завершён" (зелёный) + toast
5. Повторный клик во время выполнения — пилл "Ошибка" + toast красный
</verification>

<success_criteria>
- index.html содержит `id="logPanel"` и `id="toastContainer"`
- main.js содержит `listen('stage-status'` и `listen('stage-log'`
- main.js содержит `activeStages` Set для toggle-логики
- main.js содержит `escapeHtml()` для XSS-защиты
- Статический `stage-active` класс убран из HTML карточки plantsim
</success_criteria>

<output>
После завершения создать `.planning/phases/02-upravlenie-pajplajnom/02-02-SUMMARY.md`
</output>
