---
phase: 03-integraciya-plant-simulation
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - bratsy-tauri/dev-tools/mock-plantsim.ps1
autonomous: true
requirements:
  - INT-01
  - INT-02

must_haves:
  truths:
    - "mock-plantsim.ps1 существует и принимает аргументы /S macro.spm file.spp"
    - "Скрипт записывает results.txt с тремя ключами в work_dir .spp-файла или текущей директории"
    - "Скрипт завершается с exit code 0 после ~2 секунд задержки"
    - "Stdout содержит несколько строк — достаточно для проверки стриминга лога"
  artifacts:
    - path: "bratsy-tauri/dev-tools/mock-plantsim.ps1"
      provides: "Заглушка PlantSim.exe для разработки без реального ПО"
      contains: "load=87.3"
  key_links:
    - from: "mock-plantsim.ps1"
      to: "work_dir/results.txt"
      via: "Set-Content -Path $resultsPath"
      pattern: "load=87.3"
---

<objective>
Создать PowerShell-заглушку mock-plantsim.ps1, которая имитирует поведение PlantSimulation.exe: принимает аргументы /S и .spp-путь, пишет results.txt по контракту D-07 (key=value) и завершается с exit code 0.

Без этого артефакта разработка и тестирование планов Wave 1 и Wave 2 невозможны — реальный PlantSim.exe отсутствует в dev-среде.

Purpose: Закрыть Wave 0 gap из VALIDATION.md — создать единственный необходимый тестовый артефакт.
Output: `bratsy-tauri/dev-tools/mock-plantsim.ps1`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-integraciya-plant-simulation/03-CONTEXT.md
@.planning/phases/03-integraciya-plant-simulation/03-VALIDATION.md

<interfaces>
<!-- Контракт results.txt (D-07 из CONTEXT.md) -->
<!--
Формат: key=value построчно, UTF-8, без BOM:
  load=87.3
  throughput=42
  cycle_time=18.5

Rust-парсер (из RESEARCH.md Pattern 3) использует:
  line.split_once('=') → (k.trim(), v.trim().parse::<f32>())
  ключи: "load", "throughput", "cycle_time"

Команда запуска из Rust (D-04):
  Command::new(&plant_sim_exe).args(["/S", &plant_sim_macro, &plant_sim_path])
  → в dev-режиме plant_sim_exe = "powershell", и отдельные аргументы:
    ["-File", "mock-plantsim.ps1", "/S", "macro.spm", "file.spp"]
  ИЛИ plant_sim_exe = путь к .ps1, запускаемому напрямую через powershell.exe
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать директорию dev-tools и написать mock-plantsim.ps1</name>
  <files>bratsy-tauri/dev-tools/mock-plantsim.ps1</files>

  <read_first>
    - .planning/phases/03-integraciya-plant-simulation/03-CONTEXT.md (D-07: контракт results.txt)
    - .planning/phases/03-integraciya-plant-simulation/03-PATTERNS.md (Pattern 13: шаблон mock-скрипта)
    - .planning/phases/03-integraciya-plant-simulation/03-VALIDATION.md (Wave 0 Requirements)
  </read_first>

  <action>
Создать директорию `bratsy-tauri/dev-tools/` и файл `mock-plantsim.ps1` с точным содержимым:

```powershell
# mock-plantsim.ps1
# Заглушка PlantSimulation.exe для разработки без реального ПО.
# Принимает те же аргументы что PlantSim.exe (D-01):
#   powershell -File mock-plantsim.ps1 /S "macro.spm" "file.spp"
# Контракт results.txt соответствует D-07 из CONTEXT.md.

param(
    [string]$S = "",        # путь к .spm макросу (передаётся как /S следующим аргументом)
    [string]$SppPath = ""   # путь к .spp файлу (позиционный, 3-й аргумент)
)

Write-Output "[mock-plantsim] Запуск симуляции..."
Write-Output "[mock-plantsim] Макрос: $S"
Write-Output "[mock-plantsim] Модель: $SppPath"
Write-Output "[mock-plantsim] Шаг 1/3: инициализация..."

Start-Sleep -Seconds 1

Write-Output "[mock-plantsim] Шаг 2/3: симуляция (2000 шагов)..."

Start-Sleep -Seconds 1

Write-Output "[mock-plantsim] Шаг 3/3: запись результатов..."

# Определить work_dir — директория .spp файла или текущая директория
$workDir = if ($SppPath -and (Test-Path $SppPath)) {
    Split-Path $SppPath -Parent
} else {
    (Get-Location).Path
}

# Записать results.txt по контракту D-07 (key=value, UTF-8)
$resultsPath = Join-Path $workDir "results.txt"
$content = "load=87.3`nthroughput=42`ncycle_time=18.5"
[System.IO.File]::WriteAllText($resultsPath, $content, [System.Text.Encoding]::UTF8)

Write-Output "[mock-plantsim] Результаты записаны: $resultsPath"
Write-Output "[mock-plantsim] Завершено. Коэффициент загрузки: 87.3%, пропускная способность: 42 ед./ч, время цикла: 18.5 сек."

exit 0
```

ВАЖНО: Использовать `[System.IO.File]::WriteAllText(...)` вместо `Set-Content` — гарантирует UTF-8 без BOM, что необходимо для `std::fs::read_to_string` в Rust.

ВАЖНО: Параметр `$SppPath` — позиционный (второй параметр после `/S`). При вызове из Rust через `Command::new("powershell").args(["-File", "mock-plantsim.ps1", "/S", macro_path, spp_path])` PowerShell биндит первый аргумент (`/S`) к именованному параметру `-S`, а второй (`spp_path`) — к позиционному `$SppPath`.
  </action>

  <read_first>
    - .planning/phases/03-integraciya-plant-simulation/03-CONTEXT.md
  </read_first>

  <verify>
    <automated>
      Проверить существование файла и ключевые строки:
      Test в PowerShell:
        Test-Path bratsy-tauri/dev-tools/mock-plantsim.ps1
        Select-String -Path bratsy-tauri/dev-tools/mock-plantsim.ps1 -Pattern "load=87.3"
        Select-String -Path bratsy-tauri/dev-tools/mock-plantsim.ps1 -Pattern "throughput=42"
        Select-String -Path bratsy-tauri/dev-tools/mock-plantsim.ps1 -Pattern "cycle_time=18.5"
        Select-String -Path bratsy-tauri/dev-tools/mock-plantsim.ps1 -Pattern "exit 0"
    </automated>
  </verify>

  <acceptance_criteria>
    - Файл `bratsy-tauri/dev-tools/mock-plantsim.ps1` существует
    - Файл содержит строку `load=87.3`
    - Файл содержит строку `throughput=42`
    - Файл содержит строку `cycle_time=18.5`
    - Файл содержит строку `exit 0`
    - Файл содержит `WriteAllText` или `Set-Content` для записи results.txt
    - Файл содержит параметры `$S` и `$SppPath`
    - Ручная проверка: `powershell -File bratsy-tauri/dev-tools/mock-plantsim.ps1` завершается без ошибок и создаёт results.txt в текущей директории
  </acceptance_criteria>

  <done>mock-plantsim.ps1 создан, содержит контракт D-07 (три ключа key=value), завершается exit code 0, пишет stdout — Wave 0 gap из VALIDATION.md закрыт</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-tools → filesystem | Скрипт пишет results.txt в work_dir — только dev-среда, не production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering | mock-plantsim.ps1 → results.txt | accept | Dev-only артефакт; реальные данные от настоящего PlantSim.exe в prod |
</threat_model>

<verification>
После выполнения:
1. `Test-Path bratsy-tauri/dev-tools/mock-plantsim.ps1` → True
2. `Select-String "load=87.3" bratsy-tauri/dev-tools/mock-plantsim.ps1` → находит строку
3. Ручной запуск: `powershell -File bratsy-tauri/dev-tools/mock-plantsim.ps1` → выводит 6+ строк в stdout, создаёт results.txt в текущей директории
4. `Get-Content results.txt` → показывает load=87.3, throughput=42, cycle_time=18.5
</verification>

<success_criteria>
- `bratsy-tauri/dev-tools/mock-plantsim.ps1` существует
- Скрипт завершается с exit code 0
- Создаёт `results.txt` с тремя ключами: load, throughput, cycle_time
- Пишет не менее 4 строк в stdout (для тестирования BufReader стриминга)
- Wave 0 gap из VALIDATION.md снят
</success_criteria>

<output>
После завершения создать `.planning/phases/03-integraciya-plant-simulation/03-00-SUMMARY.md`
</output>
