---
phase: 03-integraciya-plant-simulation
plan: "01"
type: execute
wave: 1
depends_on:
  - "03-00"
files_modified:
  - bratsy-tauri/src-tauri/src/lib.rs
autonomous: true
requirements:
  - INT-01
  - INT-02

must_haves:
  truths:
    - "Settings struct содержит поля plant_sim_exe и plant_sim_macro"
    - "run_stage('plantsim') валидирует три пути через Path::exists() ДО вставки sentinel"
    - "При невалидном пути возвращается Err начинающийся с 'config:'"
    - "PlantSim.exe запускается через Command::new без PowerShell-обёртки"
    - "После child.wait() читается work_dir/results.txt и парсятся ключи load/throughput/cycle_time"
    - "Если results.txt не найден — эмитируется stage-log с '[warning]', не Err"
    - "Событие stage-results эмитируется с payload { stage, load, throughput, cycle_time }"
    - "StageResultsPayload объявлен с derive(Serialize, Clone)"
  artifacts:
    - path: "bratsy-tauri/src-tauri/src/lib.rs"
      provides: "Rust backend с реальной PlantSim интеграцией"
      contains: "pub plant_sim_exe: String"
    - path: "bratsy-tauri/src-tauri/src/lib.rs"
      contains: "StageResultsPayload"
    - path: "bratsy-tauri/src-tauri/src/lib.rs"
      contains: "stage-results"
  key_links:
    - from: "run_stage('plantsim')"
      to: "work_dir/results.txt"
      via: "std::fs::read_to_string после child.wait()"
      pattern: "read_to_string"
    - from: "results.txt parse"
      to: "stage-results event"
      via: "app_clone.emit('stage-results', StageResultsPayload)"
      pattern: "stage-results"
---

<objective>
Расширить Rust backend в lib.rs тремя связанными изменениями:
1. Добавить `plant_sim_exe` и `plant_sim_macro` в `Settings` struct
2. Объявить `StageResultsPayload` для нового события `stage-results`
3. Внутри `run_stage` добавить ветку для stage == "plantsim": валидация путей → реальный запуск PlantSim.exe → чтение results.txt → emit stage-results

Purpose: Реализовать ядро интеграции (INT-01, INT-02) — всё что делает PhantSim-запуск возможным.
Output: Модифицированный `bratsy-tauri/src-tauri/src/lib.rs`, компилируется без ошибок.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-integraciya-plant-simulation/03-CONTEXT.md
@.planning/phases/03-integraciya-plant-simulation/03-RESEARCH.md
@.planning/phases/03-integraciya-plant-simulation/03-PATTERNS.md

<interfaces>
<!-- ТЕКУЩЕЕ СОСТОЯНИЕ lib.rs — ключевые структуры и функции (верифицировано в коде) -->

```rust
// Строки 1-8: импорты (use tauri::Emitter — уже есть, std::path::PathBuf — уже есть)
use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::{Arc, Mutex};
use std::fs;
use std::io::{BufRead, BufReader};
use std::path::PathBuf;
use std::process::{Command, Stdio};
use tauri::Emitter;

// Строки 10-15: Settings struct (РАСШИРИТЬ — добавить 2 поля)
#[derive(Serialize, Deserialize, Clone, Default)]
pub struct Settings {
    pub plant_sim_path: String,
    pub work_dir: String,
    pub scripts_dir: String,
    // ДОБАВИТЬ:
    // pub plant_sim_exe: String,
    // pub plant_sim_macro: String,
}

// Строки 22-32: существующие Payload структуры (ДОБАВИТЬ StageResultsPayload после)
#[derive(Serialize, Clone)]
pub struct StageStatusPayload { pub stage: String, pub status: String }
#[derive(Serialize, Clone)]
pub struct StageLogPayload { pub stage: String, pub line: String }

// Строки 42-48: get_settings() — sync fn (NOT async) — безопасно вызывать из async fn
fn get_settings() -> Settings { ... }

// Строки 58-150: run_stage async fn
// Строка 64: allowed = ["autocad", "pdm", "excel", "plantsim", "report"]
// Строки 70-76: sentinel insert (ВАЛИДАЦИЯ ДОЛЖНА БЫТЬ ДО ЭТОГО — Pitfall #2)
// Строки 83-100: PowerShell mock (ЗАМЕНИТЬ ВЕТКОЙ для plantsim)
// Строки 108-110: stage_clone, app_clone, state_arc объявлены
// Строка 113: tauri::async_runtime::spawn_blocking(move || { ... })
// Строки 114-127: BufReader stdout стриминг
// Строки 129-135: child.wait() → status_ok
// Строки 137-140: map.remove (cleanup)
// Строки 142-146: emit stage-status done/error
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить Settings struct и добавить StageResultsPayload</name>
  <files>bratsy-tauri/src-tauri/src/lib.rs</files>

  <read_first>
    - bratsy-tauri/src-tauri/src/lib.rs (строки 1-32 — текущие структуры)
    - .planning/phases/03-integraciya-plant-simulation/03-CONTEXT.md (D-02, D-03, D-09)
    - .planning/phases/03-integraciya-plant-simulation/03-PATTERNS.md (Паттерн 1, Паттерн 2)
  </read_first>

  <action>
Внести два точечных изменения в lib.rs:

**Изменение 1 — Settings struct (строки 10-15):**
Добавить два поля ПОСЛЕ `scripts_dir`:
```rust
#[derive(Serialize, Deserialize, Clone, Default)]
pub struct Settings {
    pub plant_sim_path: String,
    pub work_dir: String,
    pub scripts_dir: String,
    pub plant_sim_exe: String,    // D-02: путь к PlantSimulation.exe
    pub plant_sim_macro: String,  // D-03: путь к .spm макросу SimTalk
}
```
Объяснение: `#[derive(Default)]` на уровне struct + `unwrap_or_default()` в `get_settings()` (строка 47) обеспечивают обратную совместимость — старый settings.json без новых полей вернёт пустые строки, не паникует.

**Изменение 2 — Добавить StageResultsPayload после строки 32 (после StageLogPayload):**
```rust
#[derive(Serialize, Clone)]
pub struct StageResultsPayload {
    pub stage: String,
    pub load: f32,
    pub throughput: f32,
    pub cycle_time: f32,
}
```
Этот payload будет использоваться в Task 2 при emit("stage-results", ...).

НЕ менять ничего другого в этом таске — только struct-декларации.
  </action>

  <verify>
    <automated>
      В PowerShell из корня проекта:
        Select-String -Path bratsy-tauri/src-tauri/src/lib.rs -Pattern "pub plant_sim_exe: String"
        Select-String -Path bratsy-tauri/src-tauri/src/lib.rs -Pattern "pub plant_sim_macro: String"
        Select-String -Path bratsy-tauri/src-tauri/src/lib.rs -Pattern "StageResultsPayload"
        Select-String -Path bratsy-tauri/src-tauri/src/lib.rs -Pattern "pub cycle_time: f32"
    </automated>
  </verify>

  <acceptance_criteria>
    - `lib.rs` содержит `pub plant_sim_exe: String`
    - `lib.rs` содержит `pub plant_sim_macro: String`
    - `lib.rs` содержит `pub struct StageResultsPayload`
    - `lib.rs` содержит `pub load: f32`
    - `lib.rs` содержит `pub throughput: f32`
    - `lib.rs` содержит `pub cycle_time: f32`
    - Settings struct по-прежнему содержит `pub plant_sim_path: String` (не удалено)
    - `#[derive(Serialize, Deserialize, Clone, Default)]` над Settings — не изменён
    - `#[derive(Serialize, Clone)]` над StageResultsPayload присутствует
  </acceptance_criteria>

  <done>Settings расширен двумя полями, StageResultsPayload объявлен — структуры готовы для Task 2</done>
</task>

<task type="auto">
  <name>Task 2: Реализовать PlantSim-ветку в run_stage</name>
  <files>bratsy-tauri/src-tauri/src/lib.rs</files>

  <read_first>
    - bratsy-tauri/src-tauri/src/lib.rs (ПОЛНЫЙ файл — нужно точно знать строки для вставки)
    - .planning/phases/03-integraciya-plant-simulation/03-CONTEXT.md (D-01, D-04, D-05, D-07, D-08, D-13, D-14)
    - .planning/phases/03-integraciya-plant-simulation/03-PATTERNS.md (Паттерны 3, 4, 5)
    - .planning/phases/03-integraciya-plant-simulation/03-RESEARCH.md (Pitfall #1, #2, Pattern 2, Pattern 3)
  </read_first>

  <action>
Модифицировать функцию `run_stage` тремя точечными вставками. КРИТИЧЕСКИ важен порядок операций (Pitfall #2).

**Вставка A — Валидация путей ПОСЛЕ allowlist-проверки (строка 67), НО ДО sentinel insert (строка 71):**

Добавить сразу после `if !allowed.contains(&stage.as_str()) { return Err(...) }` блок:

```rust
    // D-13: валидация путей для plantsim ДО резервирования sentinel (Pitfall #2 prevention)
    if stage == "plantsim" {
        let settings = get_settings(); // sync fn — безопасно в async контексте (Pitfall #1)
        if !std::path::Path::new(&settings.plant_sim_exe).exists() {
            return Err("config: PlantSimulation.exe не найден. Проверьте путь в настройках.".into());
        }
        if !std::path::Path::new(&settings.plant_sim_path).exists() {
            return Err("config: файл .spp не найден. Проверьте путь в настройках.".into());
        }
        if !std::path::Path::new(&settings.plant_sim_macro).exists() {
            return Err("config: файл .spm макроса не найден. Проверьте путь в настройках.".into());
        }
    }
```

Префикс `"config:"` ОБЯЗАТЕЛЕН — JS использует `e.startsWith('config:')` для разветвления обработки ошибок (D-13 vs D-14).

**Вставка B — Замена PowerShell mock на PlantSim ветку:**

ТЕКУЩИЙ КОД (строки 83-100) — mock скрипт + Command::new("powershell"). Заменить на условную логику:

```rust
    // Phase 3: для plantsim — реальный запуск; для остальных — mock
    let (cmd_program, cmd_args_owned): (String, Vec<String>) = if stage == "plantsim" {
        let s = get_settings(); // sync fn — OK в async (Pitfall #1: читаем ДО spawn_blocking)
        let plant_sim_exe   = s.plant_sim_exe.clone();
        let plant_sim_macro = s.plant_sim_macro.clone();
        let plant_sim_path  = s.plant_sim_path.clone();
        (plant_sim_exe, vec!["/S".into(), plant_sim_macro, plant_sim_path])
    } else {
        let script = format!(
            "for ($i=1; $i -le 5; $i++) {{ Write-Output '[{stage}] step $i/5'; Start-Sleep -Milliseconds 400 }}; Write-Output 'done'",
            stage = stage
        );
        ("powershell".into(), vec!["-ExecutionPolicy".into(), "Bypass".into(), "-Command".into(), script])
    };

    // Также читаем work_dir ДО spawn_blocking (Pitfall #1)
    let work_dir_for_results = if stage == "plantsim" {
        get_settings().work_dir.clone()
    } else {
        String::new()
    };
    let stage_is_plantsim = stage == "plantsim";

    // CR-01 fix: stderr(Stdio::null()) — предотвращает deadlock
    let mut child = Command::new(&cmd_program)
        .args(&cmd_args_owned)
        .stdout(Stdio::piped())
        .stderr(Stdio::null())
        .spawn()
        .map_err(|e| {
            let mut map = state.0.lock().unwrap();
            map.remove(&stage);
            e.to_string()
        })?;
```

**Вставка C — Чтение results.txt и emit stage-results в spawn_blocking, ПОСЛЕ child.wait():**

В блоке `tauri::async_runtime::spawn_blocking(move || { ... })`, сразу после блока `child.wait()` (строки 129-135) и ДО блока `state_arc.lock()` (строки 137-140):

```rust
        // D-08: читаем results.txt только для plantsim, после child.wait()
        if stage_is_plantsim {
            let results_path = std::path::Path::new(&work_dir_for_results).join("results.txt");
            match std::fs::read_to_string(&results_path) {
                Ok(content) => {
                    let mut load       = 0f32;
                    let mut throughput = 0f32;
                    let mut cycle_time = 0f32;
                    for line in content.lines() {
                        if let Some((k, v)) = line.split_once('=') {
                            match k.trim() {
                                "load"       => load       = v.trim().parse().unwrap_or(0.0),
                                "throughput" => throughput = v.trim().parse().unwrap_or(0.0),
                                "cycle_time" => cycle_time = v.trim().parse().unwrap_or(0.0),
                                _ => {}
                            }
                        }
                    }
                    // D-09: emit stage-results с числовыми результатами
                    let _ = app_clone.emit("stage-results", StageResultsPayload {
                        stage: "plantsim".into(),
                        load,
                        throughput,
                        cycle_time,
                    });
                }
                Err(_) => {
                    // D-08: файл отсутствует — warning в лог, не ошибка
                    let _ = app_clone.emit("stage-log", StageLogPayload {
                        stage: stage_clone.clone(),
                        line: "[warning] results.txt не найден — результаты недоступны".into(),
                    });
                }
            }
        }
```

ВАЖНО для Вставки C: переменные `stage_is_plantsim` и `work_dir_for_results` объявлены в async-части до `spawn_blocking` и захватываются как `move`. `app_clone` и `stage_clone` уже объявлены в текущем коде (строки 108-110).

ВАЖНО для компиляции: `std::path::Path` уже доступен через `std::path::PathBuf` импорт (строка 6). Добавлять отдельный `use std::path::Path` не нужно — использовать полный путь `std::path::Path::new(...)`.

После всех изменений выполнить `cargo check` из директории `bratsy-tauri/src-tauri/`:
```
cd bratsy-tauri/src-tauri && cargo check
```
Если cargo check проходит — задача выполнена. Если нет — исправить ошибки компилятора, не изменяя логику.
  </action>

  <verify>
    <automated>
      В PowerShell из корня проекта:
        Select-String -Path bratsy-tauri/src-tauri/src/lib.rs -Pattern "config: PlantSimulation.exe"
        Select-String -Path bratsy-tauri/src-tauri/src/lib.rs -Pattern "stage-results"
        Select-String -Path bratsy-tauri/src-tauri/src/lib.rs -Pattern "results.txt"
        Select-String -Path bratsy-tauri/src-tauri/src/lib.rs -Pattern "split_once"
        Select-String -Path bratsy-tauri/src-tauri/src/lib.rs -Pattern "stage_is_plantsim"
      Затем: cd bratsy-tauri/src-tauri; cargo check (должно завершиться без errors)
    </automated>
  </verify>

  <acceptance_criteria>
    - `lib.rs` содержит `"config: PlantSimulation.exe не найден"` (ровно с этим префиксом)
    - `lib.rs` содержит `"config: файл .spp не найден"` 
    - `lib.rs` содержит `"config: файл .spm макроса не найден"`
    - Валидация путей (`Path::new(...).exists()`) расположена ДО строки с `map.insert(stage.clone(), 0)` (sentinel insert) — проверить grep по порядку строк
    - `lib.rs` содержит `app_clone.emit("stage-results", StageResultsPayload`
    - `lib.rs` содержит `split_once('=')`
    - `lib.rs` содержит `"throughput" => throughput`
    - `lib.rs` содержит `"[warning] results.txt не найден"`
    - `lib.rs` содержит `args(["/S"` или `"/S".into()` в plantsim-ветке
    - `cargo check` из `bratsy-tauri/src-tauri/` завершается без `error[` строк
  </acceptance_criteria>

  <done>run_stage содержит PlantSim-ветку: валидация путей → Command::new(plant_sim_exe) → results.txt parsing → stage-results emit. Cargo check зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS invoke → Rust run_stage | Параметр `stage` пересекает IPC-границу из WebView в Rust |
| Rust → filesystem | Чтение settings.json и results.txt; запись не происходит |
| Rust → OS process | Command::new() запускает внешний процесс PlantSim.exe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | run_stage `stage` parameter | mitigate | Allowlist `["autocad","pdm","excel","plantsim","report"]` уже на строке 64; любой другой ID → Err("invalid stage") |
| T-01-02 | Tampering | Path::new(&settings.plant_sim_exe) path traversal | accept | Пути задаются через file dialog (не свободный ввод); `Path::exists()` вернёт false для несуществующих путей; локальное приложение, 1 пользователь |
| T-01-03 | Spoofing | results.txt подмена | accept | Файл в work_dir — доверенная папка на заводском ПК; MVP-риск приемлем; локальная среда |
| T-01-04 | Denial of Service | TOCTOU sentinel insert | mitigate | Проверка sentinel + insert в одном критическом разделе (строки 70-76 — уже реализовано в Phase 2) |
</threat_model>

<verification>
После выполнения обоих задач:
1. `cargo check` из `bratsy-tauri/src-tauri/` — нет errors
2. `Select-String "plant_sim_exe" lib.rs` — находит в Settings struct
3. `Select-String "stage-results" lib.rs` — находит emit и StageResultsPayload
4. `Select-String "config:" lib.rs` — находит 3 строки валидации путей
5. Smoke: `npm run tauri dev` (из bratsy-tauri/) → UI запускается, кнопка PlantSim кликабельна
6. При пустых настройках PlantSim → появляется ошибка (не краш)
</verification>

<success_criteria>
- Settings struct имеет 5 полей: plant_sim_path, work_dir, scripts_dir, plant_sim_exe, plant_sim_macro
- StageResultsPayload объявлен с load/throughput/cycle_time f32
- run_stage("plantsim") валидирует пути ДО sentinel и возвращает Err с префиксом "config:"
- PlantSim запускается через Command::new(plant_sim_exe), не через powershell
- После child.wait() читается results.txt, парсятся key=value, эмитируется stage-results
- cargo check проходит без ошибок
</success_criteria>

<output>
После завершения создать `.planning/phases/03-integraciya-plant-simulation/03-01-SUMMARY.md`
</output>
