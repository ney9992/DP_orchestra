---
phase: 04-dannye-plant-simulation
plan: 03
type: execute
wave: 1
depends_on: [04-01-PLAN.md, 04-02-PLAN.md]
files_modified:
  - bratsy-tauri/src/index.html
  - bratsy-tauri/src/main.js
autonomous: true
requirements: [INT-01, INT-02, INT-03, PIPE-04]
gap_closure: true
gaps_addressed:
  - "inputSppPath отсутствует в index.html (main.js уже ссылается на строках 465, 481, 503)"
  - "inputWorkDir отсутствует в index.html И в main.js (loadSettings + btnSave)"

must_haves:
  truths:
    - "Панель настроек содержит поле inputSppPath — пользователь может задать путь к .spp файлу"
    - "Панель настроек содержит поле inputWorkDir — пользователь может задать рабочий каталог"
    - "loadSettings загружает spp_path и work_dir из Settings в соответствующие HTML-поля"
    - "btnSave сохраняет значения inputSppPath и inputWorkDir как spp_path и work_dir"
    - "browse-btn для inputSppPath открывает диалог выбора .spp файла"
    - "browse-btn для inputWorkDir открывает диалог выбора папки (data-type=folder)"
  artifacts:
    - path: "bratsy-tauri/src/index.html"
      provides: "field-group для inputSppPath и inputWorkDir в секции PLANT SIMULATION"
      contains: "id=\"inputSppPath\""
    - path: "bratsy-tauri/src/index.html"
      provides: "field-group для inputWorkDir"
      contains: "id=\"inputWorkDir\""
    - path: "bratsy-tauri/src/main.js"
      provides: "loadSettings загружает work_dir"
      contains: "set('inputWorkDir', s.work_dir)"
    - path: "bratsy-tauri/src/main.js"
      provides: "btnSave сохраняет work_dir"
      contains: "work_dir: g('inputWorkDir')"
  key_links:
    - from: "bratsy-tauri/src/main.js btnSave"
      to: "bratsy-tauri/src/index.html #inputSppPath"
      via: "g('inputSppPath')"
      pattern: "spp_path:\\s*g\\('inputSppPath'\\)"
    - from: "bratsy-tauri/src/main.js btnSave"
      to: "bratsy-tauri/src/index.html #inputWorkDir"
      via: "g('inputWorkDir')"
      pattern: "work_dir:\\s*g\\('inputWorkDir'\\)"
    - from: "bratsy-tauri/src/main.js loadSettings"
      to: "bratsy-tauri/src/index.html #inputWorkDir"
      via: "set('inputWorkDir', s.work_dir)"
      pattern: "set\\('inputWorkDir'"
---

<objective>
Закрыть два блокирующих пробела Phase 4: добавить HTML-поля inputSppPath и inputWorkDir в секцию PLANT SIMULATION панели настроек, и прописать work_dir в loadSettings/btnSave в main.js.

Purpose: Без этих полей пользователь не может задать spp_path и work_dir через UI. Rust-валидации D-03 и D-07 срабатывают при каждом запуске, симуляция никогда не запускается, milestone v1 недостижим.

Output:
- bratsy-tauri/src/index.html — два новых field-group в секции PLANT SIMULATION
- bratsy-tauri/src/main.js — два новых присвоения в loadSettings, одно новое поле в btnSave
</objective>

<execution_context>
@e:/DP_orchestra/.planning/get-shit-done/workflows/execute-plan.md
@e:/DP_orchestra/.planning/get-shit-done/templates/summary.md
</execution_context>

<context>
@e:/DP_orchestra/.planning/ROADMAP.md
@e:/DP_orchestra/.planning/phases/04-dannye-plant-simulation/04-CONTEXT.md
@e:/DP_orchestra/.planning/phases/04-dannye-plant-simulation/04-VERIFICATION.md

<interfaces>
<!-- Существующие поля в секции PLANT SIMULATION index.html (строки 205-228) -->
<!-- Паттерн для трёх существующих field-group: -->

```html
<!-- field-group с browse-btn (data-type="file") — пример inputPlantSimShortcut: -->
<div class="field-group">
  <label class="field-label">Путь к ярлыку (.lnk)</label>
  <div class="field-row">
    <input class="field-input" id="inputPlantSimShortcut" type="text" readonly placeholder="Укажите путь к ярлыку .lnk">
    <button class="browse-btn" data-target="inputPlantSimShortcut" data-type="file">…</button>
  </div>
  <div class="field-error" id="errPlantSimShortcut">Файл не найден</div>
</div>

<!-- field-group без browse-btn (только input) — пример inputSimMethod: -->
<div class="field-group">
  <label class="field-label">Метод SimTalk (необязательно)</label>
  <div class="field-row">
    <input class="field-input" id="inputSimMethod" type="text" placeholder="Например: .UserObjects.Run">
  </div>
  <div class="field-hint">...</div>
</div>

<!-- field-group с readonly и browse-btn для folder — новый паттерн (data-type="folder"): -->
<!-- Паттерн browse-btn: data-target="{id}" data-type="folder" -->
<!-- Логика в main.js строка 498-499: if (btn.dataset.type === 'folder') { selected = await invoke('pick_folder'...) } -->
```

<!-- loadSettings в main.js (строки 460-472) — точное содержимое: -->
```javascript
async function loadSettings() {
  try {
    const s = await invoke('get_settings');
    const set = (id, val) => { const el = document.getElementById(id); if (el && val) el.value = val; };
    set('inputPlantSimShortcut', s.plant_sim_shortcut);
    set('inputSppPath',          s.spp_path);            // строка 465 — элемент уже ожидается
    set('inputSimMethod',        s.sim_method);
    set('inputVaultUrl',         s.vault_url);
    set('inputVaultToken',       s.vault_token);
    set('inputVaultPartNumber',  s.vault_part_number);
    set('inputSimTimeout', s.sim_timeout_minutes > 0 ? String(s.sim_timeout_minutes) : '');
  } catch (e) { console.error(e); }
}
```

<!-- btnSave в main.js (строки 474-491) — точное содержимое объекта settings: -->
```javascript
await invoke('save_settings', { settings: {
  ...s,
  plant_sim_shortcut:  g('inputPlantSimShortcut'),
  spp_path:            g('inputSppPath'),            // строка 481 — уже есть
  sim_method:          g('inputSimMethod'),
  vault_url:           g('inputVaultUrl'),
  vault_token:         g('inputVaultToken'),
  vault_part_number:   g('inputVaultPartNumber'),
  sim_timeout_minutes: parseInt(g('inputSimTimeout'), 10) || 0,
  // work_dir ОТСУТСТВУЕТ — нужно добавить
}});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить поля inputSppPath и inputWorkDir в index.html</name>
  <files>bratsy-tauri/src/index.html</files>

  <read_first>
    Перед редактированием прочитать братsy-tauri/src/index.html строки 200-235, чтобы
    подтвердить точную позицию вставки: после закрывающего тега </div> field-group
    inputSimTimeout (строка 228) и перед <div class="settings-section-label">VAULT PDM API</div>
    (строка 230).
  </read_first>

  <action>
    Вставить два новых field-group между строкой 228 (конец field-group inputSimTimeout)
    и строкой 230 (settings-section-label VAULT PDM API).

    ТОЧНЫЙ HTML для вставки (вставить как новые строки между строкой 228 и строкой 230):

    ```html

        <div class="field-group">
          <label class="field-label">Путь к .spp файлу</label>
          <div class="field-row">
            <input class="field-input" id="inputSppPath" type="text" readonly placeholder="Укажите путь к .spp файлу">
            <button class="browse-btn" data-target="inputSppPath" data-type="file">…</button>
          </div>
        </div>

        <div class="field-group">
          <label class="field-label">Рабочий каталог</label>
          <div class="field-row">
            <input class="field-input" id="inputWorkDir" type="text" readonly placeholder="Рабочий каталог (куда PlantSim пишет results.txt)">
            <button class="browse-btn" data-target="inputWorkDir" data-type="folder">…</button>
          </div>
        </div>
    ```

    Индент: 8 пробелов (как у соседних field-group в файле).
    Атрибут type="text" readonly — соответствует паттерну inputPlantSimShortcut.
    data-type="file" для inputSppPath — существующая ветка в browse-btn логике (строка 503 main.js).
    data-type="folder" для inputWorkDir — существующая ветка в browse-btn логике (строки 498-499 main.js).
    НЕ добавлять field-error или field-hint — у inputPlantSimShortcut есть field-error, но у inputSimMethod нет; для новых полей используем минимальный паттерн без доп. элементов.
  </action>

  <verify>
    <automated>
      cd e:/DP_orchestra && grep -c "id=\"inputSppPath\"" bratsy-tauri/src/index.html
    </automated>
    Ожидаемый результат: 1

    <automated>
      cd e:/DP_orchestra && grep -c "id=\"inputWorkDir\"" bratsy-tauri/src/index.html
    </automated>
    Ожидаемый результат: 1

    <automated>
      cd e:/DP_orchestra && grep -n "inputWorkDir" bratsy-tauri/src/index.html
    </automated>
    Ожидаемый результат: строка содержит data-target="inputWorkDir" data-type="folder"

    <automated>
      cd e:/DP_orchestra && grep -n "VAULT PDM API" bratsy-tauri/src/index.html
    </automated>
    Убедиться что VAULT PDM API секция всё ещё на месте и не сдвинулась некорректно.
  </verify>

  <acceptance_criteria>
    - grep -c 'id="inputSppPath"' bratsy-tauri/src/index.html == 1
    - grep -c 'id="inputWorkDir"' bratsy-tauri/src/index.html == 1
    - grep -c 'data-type="folder"' bratsy-tauri/src/index.html == 1 (только inputWorkDir имеет data-type="folder")
    - grep -c 'data-target="inputSppPath"' bratsy-tauri/src/index.html == 1
    - grep -c 'data-target="inputWorkDir"' bratsy-tauri/src/index.html == 1
    - Оба поля расположены внутри секции PLANT SIMULATION (до строки с "VAULT PDM API")
    - HTML-синтаксис корректен: каждый открывающий тег имеет закрывающий
  </acceptance_criteria>

  <done>
    index.html содержит два новых field-group с id="inputSppPath" (data-type="file") и
    id="inputWorkDir" (data-type="folder") в секции PLANT SIMULATION, до метки VAULT PDM API.
  </done>
</task>

<task type="auto">
  <name>Task 2: Прописать work_dir в loadSettings и btnSave в main.js</name>
  <files>bratsy-tauri/src/main.js</files>

  <read_first>
    Перед редактированием прочитать bratsy-tauri/src/main.js строки 460-491, чтобы
    подтвердить точное содержимое loadSettings и btnSave. Убедиться что:
    - строка 465: set('inputSppPath', s.spp_path) — уже существует
    - строк с inputWorkDir или work_dir в этом диапазоне нет
  </read_first>

  <action>
    Два точечных изменения:

    **Изменение A — loadSettings (строка ~466):**
    После строки `set('inputSppPath', s.spp_path);` добавить:
    ```javascript
    set('inputWorkDir',         s.work_dir);
    ```
    Выравнивание: два пробела отступа как у соседних строк, выравнивание значения по колонке
    аналогично другим set() вызовам в блоке (позиция после запятой).

    **Изменение B — btnSave (строка ~487):**
    После строки `sim_timeout_minutes: parseInt(g('inputSimTimeout'), 10) || 0,` добавить:
    ```javascript
          work_dir:            g('inputWorkDir'),
    ```
    Выравнивание: 6 пробелов отступа + выравнивание значения по колонке как у соседних
    полей в объекте (например plant_sim_shortcut, spp_path и др.).

    НЕ изменять ничего кроме этих двух мест. set('inputSppPath', ...) в loadSettings
    уже существует — не трогать.
  </action>

  <verify>
    <automated>
      cd e:/DP_orchestra && grep -n "inputWorkDir\|work_dir" bratsy-tauri/src/main.js
    </automated>
    Ожидаемый результат: минимум 2 строки:
    - одна содержит `set('inputWorkDir', s.work_dir)` в loadSettings
    - одна содержит `work_dir: g('inputWorkDir')` в btnSave

    <automated>
      cd e:/DP_orchestra && grep -c "work_dir" bratsy-tauri/src/main.js
    </automated>
    Ожидаемый результат: 2 (ровно два вхождения — loadSettings и btnSave)
  </verify>

  <acceptance_criteria>
    - grep -c "set('inputWorkDir'" bratsy-tauri/src/main.js == 1
    - grep -c "work_dir:.*g('inputWorkDir')" bratsy-tauri/src/main.js == 1
    - grep -c "set('inputSppPath'" bratsy-tauri/src/main.js == 1 (не изменялось — проверка что не сломали)
    - grep -c "spp_path:.*g('inputSppPath')" bratsy-tauri/src/main.js == 1 (не изменялось)
    - Структура объекта в btnSave синтаксически корректна (нет пропущенных запятых)
  </acceptance_criteria>

  <done>
    main.js содержит set('inputWorkDir', s.work_dir) в loadSettings и
    work_dir: g('inputWorkDir') в btnSave. Все существующие строки сохранены без изменений.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI-поля → Rust save_settings | Пути файлов/папок от пользователя пересекают границу через invoke. Rust не валидирует содержимое на инъекции — только проверяет существование (D-03, D-07). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | inputSppPath / inputWorkDir значения | accept | Поля readonly — пользователь выбирает через нативный диалог pick_file / pick_folder. Прямой ввод не возможен. |
| T-04-03-02 | Information Disclosure | work_dir путь в save_settings | accept | work_dir — локальный путь на заводском ПК, не передаётся по сети, хранится в локальном settings.json. Low-value target. |
</threat_model>

<verification>
После выполнения обоих задач выполнить итоговую проверку:

```bash
# Gap 1 закрыт: inputSppPath присутствует в HTML
grep -c 'id="inputSppPath"' e:/DP_orchestra/bratsy-tauri/src/index.html
# Ожидаемый результат: 1

# Gap 2 закрыт: inputWorkDir присутствует в HTML
grep -c 'id="inputWorkDir"' e:/DP_orchestra/bratsy-tauri/src/index.html
# Ожидаемый результат: 1

# Gap 2 закрыт: work_dir в loadSettings
grep -c "set('inputWorkDir'" e:/DP_orchestra/bratsy-tauri/src/main.js
# Ожидаемый результат: 1

# Gap 2 закрыт: work_dir в btnSave
grep -n "work_dir" e:/DP_orchestra/bratsy-tauri/src/main.js
# Ожидаемый результат: 2 строки (loadSettings + btnSave)

# Существующие поля не сломаны
grep -c "set('inputSppPath'" e:/DP_orchestra/bratsy-tauri/src/main.js
# Ожидаемый результат: 1
grep -c "spp_path:" e:/DP_orchestra/bratsy-tauri/src/main.js
# Ожидаемый результат: 1
```
</verification>

<success_criteria>
1. index.html содержит field-group с id="inputSppPath" (browse-btn data-type="file") в секции PLANT SIMULATION
2. index.html содержит field-group с id="inputWorkDir" (browse-btn data-type="folder") в секции PLANT SIMULATION
3. main.js loadSettings вызывает set('inputWorkDir', s.work_dir)
4. main.js btnSave передаёт work_dir: g('inputWorkDir') в save_settings
5. Все существующие поля (inputPlantSimShortcut, inputSppPath, inputSimMethod, inputSimTimeout, vault_*) сохранены без изменений
6. Browse-кнопки работают через существующую логику в main.js строки 493-510 без изменений кода browse-btn handler

Измеримое финальное состояние: пользователь открывает настройки → видит поля «Путь к .spp файлу» и «Рабочий каталог» → задаёт значения → сохраняет → запускает симуляцию → Rust-валидации D-03/D-07 не срабатывают → числа из results.txt появляются в UI.
</success_criteria>

<output>
После завершения создать `.planning/phases/04-dannye-plant-simulation/04-03-SUMMARY.md`

Формат: используй шаблон @e:/DP_orchestra/.planning/get-shit-done/templates/summary.md
Включить: какие gap-ы закрыты, какие строки добавлены в index.html и main.js, результаты grep-проверок.
</output>
