---
phase: 04-dannye-plant-simulation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - bratsy-tauri/src/index.html
  - bratsy-tauri/src/main.js
  - bratsy-tauri/docs/simtalk-template.md
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03

must_haves:
  truths:
    - "Панель настроек содержит поле «Таймаут симуляции (мин)» с placeholder «2»"
    - "Сохранение настроек записывает sim_timeout_minutes из inputSimTimeout в Settings"
    - "Загрузка настроек заполняет inputSimTimeout из settings.sim_timeout_minutes"
    - "invoke('run_plantsim') вызывается с параметром sppPath из settings.spp_path и workDir из settings.work_dir"
    - "При Err('config: ...') от run_plantsim — showConfigError() показывает текст ошибки + кнопку «Открыть настройки»"
    - "Файл bratsy-tauri/docs/simtalk-template.md существует и содержит готовый SimTalk-шаблон"
  artifacts:
    - path: "bratsy-tauri/src/index.html"
      provides: "field-group для sim_timeout_minutes в секции PLANT SIMULATION"
      contains: "inputSimTimeout"
    - path: "bratsy-tauri/src/main.js"
      provides: "showConfigError(), передача sppPath/workDir в run_plantsim"
      contains: "showConfigError"
    - path: "bratsy-tauri/docs/simtalk-template.md"
      provides: "Документация SimTalk-шаблона для пользователя"
      contains: "getCommandLineArg"
  key_links:
    - from: "btnSave handler (main.js)"
      to: "inputSimTimeout"
      via: "g('inputSimTimeout')"
      pattern: "inputSimTimeout"
    - from: "runReal('plantsim') (main.js)"
      to: "run_plantsim Rust command"
      via: "invoke('run_plantsim', { lnkPath, sppPath, method, workDir })"
      pattern: "sppPath.*workDir|workDir.*sppPath"
    - from: "run_plantsim Err('config: ...')"
      to: "showConfigError()"
      via: "catch block в runReal — разбор сообщения"
      pattern: "config:"
---

<objective>
Фронтенд-часть Phase 4: добавить поле таймаута в настройки, передать sppPath и workDir в run_plantsim, показывать ошибки конфигурации через showConfigError(), создать SimTalk-документацию.

Purpose: После Plan 01 Rust-команда run_plantsim ожидает sppPath и workDir из JS — Plan 02 завершает цепочку. Пользователь видит понятное сообщение когда настройки не заданы. SimTalk-шаблон устраняет необходимость дополнительной документации.

Output: index.html с полем inputSimTimeout, обновлённый main.js (loadSettings, saveSettings, runReal, showConfigError), файл bratsy-tauri/docs/simtalk-template.md.
</objective>

<execution_context>
@e:/DP_orchestra/bratsy-tauri/.planning/../../../.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@e:/DP_orchestra/.planning/ROADMAP.md
@e:/DP_orchestra/.planning/phases/04-dannye-plant-simulation/04-CONTEXT.md
@e:/DP_orchestra/.planning/phases/04-dannye-plant-simulation/04-RESEARCH.md
@e:/DP_orchestra/.planning/phases/04-dannye-plant-simulation/04-01-SUMMARY.md

<interfaces>
<!-- Ключевые части frontend. Прочитать перед изменением. -->

Текущая панель настроек (index.html, строки 203-251):
- Секция "PLANT SIMULATION" содержит: inputPlantSimShortcut (.lnk), inputSimMethod
- Секция "VAULT PDM API" содержит: inputVaultUrl, inputVaultToken, inputVaultPartNumber
- Паттерн field-group:
  ```html
  <div class="field-group">
    <label class="field-label">Метка</label>
    <div class="field-row">
      <input class="field-input" id="inputXxx" type="text" placeholder="...">
    </div>
    <div class="field-hint">Подсказка</div>
  </div>
  ```

Текущий loadSettings (main.js, строки 432-443):
```js
async function loadSettings() {
  try {
    const s = await invoke('get_settings');
    const set = (id, val) => { const el = document.getElementById(id); if (el && val) el.value = val; };
    set('inputPlantSimShortcut', s.plant_sim_shortcut);
    set('inputSppPath',          s.spp_path);
    set('inputSimMethod',        s.sim_method);
    set('inputVaultUrl',         s.vault_url);
    set('inputVaultToken',       s.vault_token);
    set('inputVaultPartNumber',  s.vault_part_number);
  } catch (e) { console.error(e); }
}
```

Текущий btnSave handler (main.js, строки 445-461):
```js
document.getElementById('btnSave').addEventListener('click', async () => {
  try {
    const s = await invoke('get_settings');
    const g = id => document.getElementById(id)?.value || '';
    await invoke('save_settings', { settings: {
      ...s,
      plant_sim_shortcut: g('inputPlantSimShortcut'),
      spp_path:           g('inputSppPath'),
      sim_method:         g('inputSimMethod'),
      vault_url:          g('inputVaultUrl'),
      vault_token:        g('inputVaultToken'),
      vault_part_number:  g('inputVaultPartNumber'),
    }});
    showToast('Настройки сохранены', 'success');
    settingsOverlay.classList.remove('open');
  } catch { showToast('Ошибка сохранения', 'error'); }
});
```

Текущий runReal plantsim case (main.js, строки 404-415):
```js
case 'plantsim': {
  const s = await invoke('get_settings');
  const lnkPath = await invoke('find_plantsim_shortcut');
  const method = s.sim_method || '';
  const waiter = waitForStage('plantsim');
  await invoke('run_plantsim', { lnkPath, method });
  await waiter;
  break;
}
```

Текущий showConfigError отсутствует в main.js — нужно добавить.
Паттерн из Phase 3 D-13: при Err("config: ...") показать диалог с кнопкой «Открыть настройки».
Функция openSettings() в main.js реализована через `settingsOverlay.classList.add('open')`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: index.html — поле таймаута + main.js — loadSettings, saveSettings, showConfigError, runReal</name>
  <files>bratsy-tauri/src/index.html, bratsy-tauri/src/main.js</files>

  <read_first>
    - bratsy-tauri/src/index.html (строки 203-251 — секция настроек, field-group паттерн)
    - bratsy-tauri/src/main.js (строки 418-490 — loadSettings, btnSave, runReal plantsim case)
    - .planning/phases/04-dannye-plant-simulation/04-CONTEXT.md (D-09 — таймаут поле, D-03/D-07 — showConfigError, D-08 — sppPath передача)
    - .planning/phases/03-integraciya-plant-simulation/03-CONTEXT.md (D-13 — паттерн диалога ошибки)
  </read_first>

  <action>
**Изменение 1 — index.html: добавить поле inputSimTimeout:**

В секции PLANT SIMULATION (после `inputSimMethod` field-group, перед `settings-section-label` VAULT PDM API), добавить новый field-group:

```html
        <div class="field-group">
          <label class="field-label">Таймаут симуляции (мин)</label>
          <div class="field-row">
            <input class="field-input" id="inputSimTimeout" type="number" min="1" max="999" placeholder="2">
          </div>
          <div class="field-hint">Через сколько минут принудительно остановить PlantSim (0 = 2 мин)</div>
        </div>
```

Вставить это поле между закрывающим `</div>` field-group inputSimMethod и строкой `<div class="settings-section-label">VAULT PDM API</div>`.

**Изменение 2 — main.js: добавить showConfigError():**

В раздел `// ── Настройки` (после `const settingsOverlay = document.getElementById('settingsOverlay');`), добавить функцию:

```js
// Показывает ошибку конфигурации (паттерн D-13 Phase 3)
// msg — строка ошибки от Rust (начинается с "config: ...")
function showConfigError(msg) {
  // Убрать префикс "config: " для отображения
  const display = msg.startsWith('config: ') ? msg.slice(8) : msg;
  const confirmed = confirm(display + '\n\nОткрыть настройки?');
  if (confirmed) {
    settingsOverlay.classList.add('open');
    loadSettings();
  }
}
```

**Изменение 3 — main.js: обновить loadSettings для inputSimTimeout:**

В функции `loadSettings`, добавить строку для sim_timeout_minutes после последнего `set(...)`:
```js
    set('inputSimTimeout', s.sim_timeout_minutes > 0 ? String(s.sim_timeout_minutes) : '');
```

**Изменение 4 — main.js: обновить btnSave для sim_timeout_minutes:**

В btnSave handler, в объекте `settings: { ...s, ... }`, добавить:
```js
      sim_timeout_minutes: parseInt(g('inputSimTimeout'), 10) || 0,
```

**Изменение 5 — main.js: обновить runReal plantsim case:**

Заменить текущий `case 'plantsim'` блок (строки 404-415) на:
```js
    case 'plantsim': {
      const s = await invoke('get_settings');
      // D-07/D-03: find_plantsim_shortcut проверяет lnk — если ошибка config: — показать диалог
      let lnkPath;
      try {
        lnkPath = await invoke('find_plantsim_shortcut');
      } catch (e) {
        const msg = String(e.message || e);
        if (msg.startsWith('config:')) { showConfigError(msg); }
        else { clog(msg, 'err', 'plantsim'); }
        throw new Error(msg);
      }
      const method = s.sim_method || '';
      // D-08: передаём sppPath и workDir из settings в run_plantsim
      const waiter = waitForStage('plantsim');
      try {
        await invoke('run_plantsim', { lnkPath, method, sppPath: s.spp_path || '', workDir: s.work_dir || '' });
      } catch (e) {
        const msg = String(e.message || e);
        if (msg.startsWith('config:')) { showConfigError(msg); }
        else { clog(msg, 'err', 'plantsim'); }
        throw new Error(msg);
      }
      await waiter;
      break;
    }
```

ВАЖНО: Обновить сигнатуру Rust-команды run_plantsim — она принимает `lnk_path`, `method`. Параметры `spp_path` и `work_dir` Rust читает из `get_settings()` ВНУТРИ функции (D-08 — см. Задачу 1 плана 04-01). Поэтому JS НЕ должен передавать `sppPath` и `workDir` как параметры invoke — Rust-функция их не принимает. Убрать `sppPath` и `workDir` из invoke-вызова:

```js
      try {
        await invoke('run_plantsim', { lnkPath, method });
      } catch (e) {
```

Таким образом, финальный case планируемый к выполнению:
```js
    case 'plantsim': {
      const s = await invoke('get_settings');
      let lnkPath;
      try {
        lnkPath = await invoke('find_plantsim_shortcut');
      } catch (e) {
        const msg = String(e.message || e);
        if (msg.startsWith('config:')) { showConfigError(msg); }
        else { clog(msg, 'err', 'plantsim'); }
        throw new Error(msg);
      }
      const method = s.sim_method || '';
      const waiter = waitForStage('plantsim');
      try {
        await invoke('run_plantsim', { lnkPath, method });
      } catch (e) {
        const msg = String(e.message || e);
        if (msg.startsWith('config:')) { showConfigError(msg); }
        else { clog(msg, 'err', 'plantsim'); }
        throw new Error(msg);
      }
      await waiter;
      break;
    }
```
  </action>

  <verify>
    <automated>cd e:/DP_orchestra/bratsy-tauri && npm run tauri build -- --no-bundle 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "inputSimTimeout" bratsy-tauri/src/index.html` находит input элемент с type="number"
    - `grep -n "inputSimTimeout" bratsy-tauri/src/main.js` находит минимум 2 строки (loadSettings + btnSave)
    - `grep -n "showConfigError" bratsy-tauri/src/main.js` находит объявление функции и минимум 2 вызова (в find_plantsim_shortcut catch и run_plantsim catch)
    - `grep -n "config:" bratsy-tauri/src/main.js` находит проверку `msg.startsWith('config:')`
    - `grep -n "sim_timeout_minutes" bratsy-tauri/src/main.js` находит строку в btnSave со `parseInt`
    - `grep -n "Таймаут симуляции" bratsy-tauri/src/index.html` находит label поля
    - `cargo build` в bratsy-tauri/src-tauri/ завершается без ошибок (Rust не изменился, проверка на регрессию)
  </acceptance_criteria>

  <done>index.html содержит поле inputSimTimeout (type="number", placeholder="2") в секции PLANT SIMULATION. loadSettings заполняет inputSimTimeout из settings.sim_timeout_minutes. btnSave сохраняет sim_timeout_minutes через parseInt. showConfigError() показывает confirm-диалог с кнопкой «Открыть настройки». runReal plantsim перехватывает config:-ошибки из find_plantsim_shortcut и run_plantsim через showConfigError(). Invoke run_plantsim вызывается только с { lnkPath, method } — spp_path и work_dir Rust читает сам из get_settings().</done>
</task>

<task type="auto">
  <name>Задача 2: SimTalk-шаблон — bratsy-tauri/docs/simtalk-template.md</name>
  <files>bratsy-tauri/docs/simtalk-template.md</files>

  <read_first>
    - .planning/phases/04-dannye-plant-simulation/04-RESEARCH.md (строки 491-532 — готовый SimTalk-шаблон, строки 96-142 — FileInterface документация, строки 147-168 — getCommandLineArg шаблон)
    - .planning/phases/04-dannye-plant-simulation/04-CONTEXT.md (D-01 — getCommandLineArg("--workdir"), D-06 — полная строка Arguments, D-05 — формат results.txt)
  </read_first>

  <action>
Создать директорию `bratsy-tauri/docs/` (если не существует) и файл `bratsy-tauri/docs/simtalk-template.md`.

Содержимое файла:

```markdown
# SimTalk-шаблон для DP_orchestra

Данный шаблон описывает SimTalk-метод, который должен быть создан в модели Plant Simulation для корректной работы с DP_orchestra.

## Требования к модели

1. **File > Model Settings > General > Security**: снять галочку "Prohibit access to the computer"
2. Создать объект `FileInterface` в папке `InformationFlow` модели
3. Создать метод `.UserObjects.printed` в папке `UserObjects`

## Контракт результатов (results.txt)

После завершения симуляции метод должен записать в `{work_dir}\results.txt` строки в формате `ключ=значение`:

```
load=87.3
throughput=42
cycle_time=18.5
```

DP_orchestra парсит эти строки и показывает значения в панели отчёта. Ключи `load`, `throughput`, `cycle_time` — фиксированный контракт.

## Вариант 1: Получить work_dir через getCommandLineArg (рекомендуется)

DP_orchestra передаёт `--workdir "C:\путь"` в аргументах при запуске. SimTalk читает это значение:

```simtalk
-- Метод: .UserObjects.printed
-- Запускается через: PlantSimulation.exe -f "model.spp" /E .UserObjects.printed --workdir "C:\bratsy_work"

var fi : object
var workDir : string
var found : boolean

-- Читаем рабочую директорию из аргументов командной строки
found := getCommandLineArg("--workdir", workDir)
if not found
    -- Fallback: писать рядом с .spp файлом
    workDir := getCurrentDirectory
end

-- Запустить симуляцию
EventController.startSim
-- EventController.startSim блокирует до завершения

-- Записать результаты
fi := .InformationFlow.FileInterface
fi.FileName := workDir + "\\results.txt"
fi.Encoding := "UTF-8"
fi.open
-- ЗАМЕНИТЬ на реальные атрибуты объектов вашей модели:
fi.writeLn("load=" + num_to_str(Station.statPercBusy))
fi.writeLn("throughput=" + num_to_str(Sink.statNumExited))
fi.writeLn("cycle_time=" + num_to_str(Station.statTimeProc))
fi.close     -- ОБЯЗАТЕЛЬНО до exitApplication

exitApplication
```

## Вариант 2: Хардкод пути (быстрее, но не переносимо)

```simtalk
-- Метод: .UserObjects.printed

var fi : object

EventController.startSim

fi := .InformationFlow.FileInterface
fi.FileName := "C:\\bratsy_work\\results.txt"   -- двойной backslash обязателен в SimTalk
fi.Encoding := "UTF-8"
fi.open
fi.writeLn("load=" + num_to_str(Station.statPercBusy))
fi.writeLn("throughput=" + num_to_str(Sink.statNumExited))
fi.writeLn("cycle_time=" + num_to_str(Station.statTimeProc))
fi.close

exitApplication
```

## Вариант 3: Через endSim (для длинных симуляций)

```simtalk
-- Метод: .Models.Frame.endSim
-- Plant Simulation вызывает автоматически в конце симуляции

var fi : object
var workDir : string

getCommandLineArg("--workdir", workDir)
if workDir = ""
    workDir := getCurrentDirectory
end

fi := .InformationFlow.FileInterface
fi.FileName := workDir + "\\results.txt"
fi.Encoding := "UTF-8"
fi.open
fi.writeLn("load=" + num_to_str(Station.statPercBusy))
fi.writeLn("throughput=" + num_to_str(Sink.statNumExited))
fi.writeLn("cycle_time=" + num_to_str(Station.statTimeProc))
fi.close     -- СНАЧАЛА close, ПОТОМ exitApplication

exitApplication
```

## Диагностика

### results.txt не создаётся

1. Проверить: File > Model Settings > General > Security > "Prohibit access to the computer" = OFF
2. Проверить что `fi.open` возвращает true (добавить проверку: `if not fi.open then ... end`)
3. Попробовать писать в папку рядом с .spp (getCurrentDirectory)

### PlantSim открывается, но метод не выполняется

1. Проверить точный путь метода: `/E .UserObjects.printed` — точка перед UserObjects обязательна
2. Открыть PlantSim вручную → Tools → Check Model (F9) — ошибки в методе

### Кодировка: кракозябры в results.txt

Добавить явно: `fi.Encoding := "UTF-8"` до `fi.open`

### Симуляция не останавливается

Убедиться что в endSim или в .UserObjects.printed есть `exitApplication` ПОСЛЕ `fi.close`.

## Ручная проверка перед запуском из DP_orchestra

```cmd
"C:\path\to\PlantSimulation16.exe" -f "C:\path\to\model.spp" /E .UserObjects.printed --workdir "C:\bratsy_work"
```

Ожидание: PlantSim открывается, симуляция запускается, PlantSim закрывается. В `C:\bratsy_work\results.txt` появляются три строки с числами.
```

Примечание: файл создать инструментом Write. Директорию `bratsy-tauri/docs/` создать автоматически (Write создаёт родительские директории).
  </action>

  <verify>
    <automated>Test-Path "e:/DP_orchestra/bratsy-tauri/docs/simtalk-template.md"</automated>
  </verify>

  <acceptance_criteria>
    - `Test-Path "e:/DP_orchestra/bratsy-tauri/docs/simtalk-template.md"` возвращает True
    - `Select-String "getCommandLineArg" "e:/DP_orchestra/bratsy-tauri/docs/simtalk-template.md"` находит минимум 2 совпадения
    - `Select-String "exitApplication" "e:/DP_orchestra/bratsy-tauri/docs/simtalk-template.md"` находит минимум 3 совпадения (по одному в каждом варианте)
    - `Select-String "fi.close" "e:/DP_orchestra/bratsy-tauri/docs/simtalk-template.md"` находит строки с комментарием про порядок close/exitApplication
    - `Select-String "results.txt" "e:/DP_orchestra/bratsy-tauri/docs/simtalk-template.md"` находит упоминание формата контракта
    - `Select-String "Prohibit access" "e:/DP_orchestra/bratsy-tauri/docs/simtalk-template.md"` находит инструкцию по настройке безопасности модели
  </acceptance_criteria>

  <done>Файл bratsy-tauri/docs/simtalk-template.md создан с тремя рабочими вариантами SimTalk-кода (getCommandLineArg, хардкод, endSim), секцией диагностики питфоллов и инструкцией по ручной проверке.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → inputSimTimeout | Числовое поле, type="number" в HTML, parseInt в JS |
| JS invoke → Rust run_plantsim | Параметры lnkPath, method — существующие, валидируются в Rust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-06 | Tampering | inputSimTimeout → sim_timeout_minutes | accept | type="number" + parseInt(_, 10) обрезает нечисловой ввод. Rust-сторона fallback на 2 минуты при 0 значении (D-09). |
| T-4-07 | Information Disclosure | showConfigError показывает путь spp_path | accept | Путь показывается пользователю на его же машине — не раскрытие тайны. Заводской ПК — одиночный пользователь. |
</threat_model>

<verification>
После выполнения обоих задач в плане 04-02:

1. В браузере DevTools (или `npm run tauri dev`): открыть настройки → видеть поле "Таймаут симуляции (мин)"
2. В `bratsy-tauri/src/main.js`: `grep -c "showConfigError" main.js` >= 3
3. В `bratsy-tauri/src/index.html`: `grep -c "inputSimTimeout" index.html` >= 1
4. `Test-Path bratsy-tauri/docs/simtalk-template.md` = True
5. `cargo build` в bratsy-tauri/src-tauri/ завершается без ошибок
</verification>

<success_criteria>
- index.html содержит поле inputSimTimeout в секции PLANT SIMULATION (field-group паттерн, type="number", placeholder="2")
- loadSettings заполняет inputSimTimeout; btnSave сохраняет sim_timeout_minutes через parseInt
- showConfigError(msg) показывает confirm-диалог; при подтверждении открывает панель настроек
- runReal('plantsim') перехватывает config:-ошибки из find_plantsim_shortcut и run_plantsim, вызывает showConfigError
- invoke('run_plantsim') вызывается с { lnkPath, method } — без sppPath/workDir (Rust читает их сам)
- bratsy-tauri/docs/simtalk-template.md содержит 3 варианта SimTalk-кода с getCommandLineArg, хардкодом и endSim
- SimTalk-шаблон содержит правильный порядок: fi.close ПЕРЕД exitApplication
- SimTalk-шаблон содержит предупреждение о настройке "Prohibit access to the computer"
</success_criteria>

<output>
После завершения создать `.planning/phases/04-dannye-plant-simulation/04-02-SUMMARY.md`:

```markdown
---
phase: 04-dannye-plant-simulation
plan: 02
completed: YYYY-MM-DD
status: done
---

# Summary: Frontend + SimTalk документация Phase 4

## Выполнено

- index.html: поле inputSimTimeout добавлено в секцию PLANT SIMULATION (D-09)
- main.js: loadSettings заполняет inputSimTimeout
- main.js: btnSave сохраняет sim_timeout_minutes через parseInt
- main.js: showConfigError() — confirm-диалог + «Открыть настройки» (паттерн D-13 Phase 3)
- main.js: runReal('plantsim') перехватывает config:-ошибки через showConfigError
- bratsy-tauri/docs/simtalk-template.md — SimTalk-шаблон (3 варианта + диагностика)

## Файлы изменены

- bratsy-tauri/src/index.html
- bratsy-tauri/src/main.js

## Файлы созданы

- bratsy-tauri/docs/simtalk-template.md

## Milestone v1 статус

После планов 04-01 и 04-02 все технические изменения Phase 4 выполнены.
Сквозной тест на реальном PlantSim — следующий шаг (заводской ПК).
```
</output>
